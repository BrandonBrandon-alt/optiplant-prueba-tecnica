package co.com.zenvory.inventario.inventory.domain.model;

/**
 * Define si el movimiento suma (INGRESO) o resta (RETIRO) del inventario local.
 */
public enum MovementType {
    INGRESO,
    RETIRO
}
